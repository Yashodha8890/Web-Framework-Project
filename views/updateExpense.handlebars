
<div class="container-activity">
    <div class="row">
        <div class="col-sm-3 flex-column">
            <nav class="nav flex-column">
            <a href="#"><img src="images/Logo DRT.jpg" alt="Logo" href="index.handlebars"></a>
            <a class="bg-green-600 text-white w-full py-2 rounded-lg hover:bg-green-500 flex justify-center items-center"
 style="font-size:25px"href="index.handlebars">Home</a>
            <a class="bg-green-600 text-white w-full py-2 rounded-lg hover:bg-green-500 flex justify-center items-center"
" style="font-size:25px"href="/activityTracker">Activity Tracker</a>
            <a class="bg-green-600 text-white w-full py-2 rounded-lg hover:bg-green-500 flex justify-center items-center"
" style="font-size:25px"href="/addExpense">Money Tracker</a>
            <a class="bg-green-600 text-white w-full py-2 rounded-lg hover:bg-green-500 flex justify-center items-center"
" style="font-size:25px"href="">Dashboard</a>
            <a class="bg-green-600 text-white w-full py-2 rounded-lg hover:bg-green-500 flex justify-center items-center"
" style="font-size:25px"href="">Profile</a>
          
        </div>
  <div class="bg-white rounded-2xl shadow-xl p-6 max-w-2xl w-full">
    <h1 class="text-2xl font-bold mb-4 text-center">Update Expense</h1>
    
    <form action="/updateExpense/{{expense._id}}" method="POST" class="space-y-4">
      <div>
        <label class="block mb-1 font-semibold">Amount</label>
        
        <input type="number" name="amount" value="{{expense.amount}}" required class="w-full border border-gray-300 p-2 rounded" />
      </div>

      <div>
        <label class="block mb-1 font-semibold">Category</label>
        <input type="text" name="category" value="{{expense.category}}" required class="w-full border border-gray-300 p-2 rounded" />
      </div>

      <div>
        <label class="block mb-1 font-semibold">Payment Method</label>
        <input type="text" name="paymentMethod" value="{{expense.paymentMethod}}" required class="w-full border border-gray-300 p-2 rounded" />
      </div>

      <div>
        <label class="block mb-1 font-semibold">Date</label>
        <input type="date" name="date" value="{{expense.date}}" required class="w-full border border-gray-300 p-2 rounded" />
      </div>

      <div>
        <label class="block mb-1 font-semibold">Notes</label>
        <textarea name="notes" class="w-full border border-gray-300 p-2 rounded">{{expense.notes}}</textarea>
      </div>

      <div class="text-center">
        <button type="submit" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-500">Update Expense</button>
      </div>
    </form>
   
  </div>
</body>
</html>
